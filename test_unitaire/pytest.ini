
# ════════════════════════════════════════════════════════════════════════
# FILE 7: pytest.ini
# Configuration pytest (à la racine du projet)
# ════════════════════════════════════════════════════════════════════════

"""
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Options d'affichage
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    
# Markers personnalisés
markers =
    unit: Tests unitaires
    integration: Tests d'intégration
    performance: Tests de performance
    quality: Tests de qualité données
    slow: Tests lents (> 1s)
    
# Coverage
[coverage:run]
source = src
omit = 
    */tests/*
    */venv/*
    
[coverage:report]
precision = 2
show_missing = True
skip_covered = False
"""

